import { defineConfig } from "vite";

export default defineConfig({
  // Rutas relativas para que el build funcione tanto en la raíz como en
  // https://andrehh-dotcom.github.io/Portafolio/.
  base: "./",
});
